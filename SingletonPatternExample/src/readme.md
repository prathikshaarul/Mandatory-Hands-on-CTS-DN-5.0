# Singleton Pattern Example

This exercise demonstrates the Singleton Design Pattern in Java using a `Logger` class.

## Files

- `src/Logger.java` - Singleton Logger class
- `src/SingletonTest.java` - Tests whether only one Logger object is created

## Run

```bash
cd SingletonPatternExample/src
javac Logger.java SingletonTest.java
java SingletonTest